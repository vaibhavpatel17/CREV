#include<stdio.h>
int main(){
    int rows;
    int columns;
    for(rows=1;rows<=5;rows++){
        for(columns=1;columns<=rows;columns++){
            printf("%d",columns); // take the value of the column and print that 
            
        
        }
        printf("\n");
    
    }
    return 0;
}